app:
  - api: .

runtime:
  python: 3.10

build:
  commands:
    - pip install -r requirements.txt
    - python manage.py migrate
    - python manage.py collectstatic --noinput

serve:
  - url: /.*
    dest: snapfeast.wsgi:application --bind 0.0.0.0:8000